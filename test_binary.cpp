//
// Created by blockchain1 on 12/5/19.
//
#include <fstream>
#include <cstring>
#include <iostream>

using namespace std;

/*int main(){
    char input[100];
    strcpy(input, "trying binary file");
    fstream file ("try", ios::binary|ios::in|ios::out|ios::trunc);
    if(!file.is_open()){
        cout<< "Error while opening the file";
    } else {
        int length = strlen(input);
        for(int counter = 0; counter <= length; counter++) {
            file.put(input[counter]);
        }

        file.close();
    }
}*/

